# Unified dependencies for the whole workspace (lowlight_image_enhancement + NAFNet + SwinIR + UNet)
# Target: Windows 10/11, Python >=3.8, CUDA 11.8 (optional) with PyTorch 2.0.1
# Rationale:
# - Pin core stack to a stable combo (torch 2.0.1 + torchvision 0.15.2)
# - Keep numpy/scipy within versions compatible with torch and scikit-image
# - Include SwinIR's timm; include fvcore+iopath for FLOPs counter in metrics
# - Prefer stable tensorboard instead of tb-nightly
# - Exclude training-only extras (wandb, yapf) to avoid conflicts unless explicitly needed

# --- Core DL stack ---
torch==2.0.1
# If you need CUDA wheels, install from https://download.pytorch.org/whl/torch_stable.html
# Example (CUDA 11.8): pip install --index-url https://download.pytorch.org/whl/cu118 torch==2.0.1+cu118

torchvision==0.15.2
# torchaudio is not strictly required

# --- Array & SciPy stack ---
numpy==1.24.4
scipy==1.10.1

# --- Image & CV utils ---
Pillow==9.5.0
opencv-python==4.8.1.78
scikit-image==0.21.0
scikit-learn==1.3.2

# --- Training/metrics utilities ---
tqdm==4.66.1
einops==0.7.0
kornia==0.6.12
lpips==0.1.4
torchmetrics==1.2.0

# --- Logging/IO/config ---
addict==2.4.0
future==0.18.3
lmdb==1.4.1
pyyaml==6.0.1
requests==2.31.0
# Prefer stable tensorboard instead of tb-nightly
tensorboard==2.14.0

# --- FLOPs/params analysis ---
fvcore==0.1.5.post20221221
iopath==0.1.10
yacs==0.1.8
termcolor==2.3.0
tabulate==0.9.0

# --- SwinIR dependency ---
# timm is used by SwinIR (timm.models.layers)
timm==0.9.5

# --- Optional/only if needed ---
# matplotlib==3.7.3  # Only required if you use UNet's visualization utilities
# wandb==0.13.5      # Training-only; avoid unless you actually use it

# Notes:
# - Keep one and only one Pillow entry (avoid duplicates in sub-repos).
# - If you hit CUDA/driver mismatch, fall back to CPU wheels of torch/torchvision.
# - If you already have a conda env with newer numpy, adjust scipy/skimage accordingly.
